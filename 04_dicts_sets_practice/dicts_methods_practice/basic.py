student = {"name": "Ravi","age": 20,"marks": 88,"city": "Delhi"}
print(f"{list(student.keys())}, length={len(list(student.keys()))}")
print(f"{list(student.values())}, length={len(list(student.values()))}")
print(f"{list(student.items())}, length={len(list(student.items()))}")